# Markdown Insight Demo

This is a small demo file for the `markdown` insight.

## Lists

- One item
- Another item
  - Nested item

## Table

| Model | Latency (ms) | Notes |
|:------|-------------:|:------|
| Small |          120 | Fast |
| Large |          950 | Higher quality |

## Code

```python
def hello(name: str) -> str:
    return f"Hello, {name}!"

print(hello("chanterelle"))
```

Inline code like `pip install` should render nicely too.
